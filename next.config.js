/** @type {import('next').NextConfig} */
const nextConfig = {
  // This file helps Vercel recognize this as a Next.js project
  // The actual Next.js app is in the frontend directory
};

module.exports = nextConfig;
