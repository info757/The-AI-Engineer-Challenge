[phases.setup]
nixPkgs = ["python311"]

[phases.install]
cmds = [
  "cd api && python -m venv venv",
  "cd api && source venv/bin/activate && pip install -r requirements.txt"
]

[start]
cmd = "cd api && source venv/bin/activate && python -m uvicorn simple_app:app --host 0.0.0.0 --port $PORT"
